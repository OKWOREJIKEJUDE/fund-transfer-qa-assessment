{
  "name": "fund-transfer-automation",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report reports"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
